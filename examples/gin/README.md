# Gin example

This is a separate Go module so Gin does not become a dependency of the core
library. Run it with `go run .`, visit `http://localhost:8080/healthz`, then
press Ctrl+C to initiate graceful shutdown.
